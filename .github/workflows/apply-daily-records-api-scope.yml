name: Apply Daily Records API Scope Patch

on:
  push:
    branches: [feature/daily-records-hub]
    paths:
      - 'scripts/apply-daily-records-api-scope.mjs'
      - '.github/workflows/apply-daily-records-api-scope.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/daily-records-hub
          fetch-depth: 0
      - name: Apply deterministic API scope patch
        run: node scripts/apply-daily-records-api-scope.mjs
      - name: Commit product code
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add frontend/src/lib/knowledgeTreeApi.ts frontend/src/components/daily-records/DailyJournalView.tsx
          if git diff --cached --quiet; then
            echo "API scope patch already applied"
            exit 0
          fi
          git commit -m "fix(daily-records): query personal journal tree explicitly"
          git push origin HEAD:feature/daily-records-hub
